"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTransforms = void 0;
const node_fs_1 = require("node:fs");
const node_path_1 = require("node:path");
const getTransforms = () => (0, node_fs_1.readdirSync)((0, node_path_1.join)(__dirname, "..", "transforms"), { withFileTypes: true })
    .filter((dirent) => dirent.isDirectory())
    .map((dirent) => dirent.name)
    .map((dirName) => {
    const { name, description, options } = require(`../transforms/${dirName}/transform`);
    return { name, description, options };
});
exports.getTransforms = getTransforms;
